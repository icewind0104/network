{% extends "ipmanage/base.html" %}

{% block body %}
<form action="/net/deal_ip_modify/" method="POST">
{% csrf_token %}
    <div>ip address: <input type=text name="description" value="{{IP.ip_addr}}"/></div>
    <div>user: <input type=text name="mask" value="{%if IP.person %}{{IP.person}}{%endif%}"/></div>
    <div>network: <input type=text name="gateway" value="{{net.desc}}"/></div>
    <div><input type="submit" value="submit"></div>
</form>

<div>
{% if error %}
    {{ error }}
{% endif %}
</div>
{% endblock %}
